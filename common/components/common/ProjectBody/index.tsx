import { useId } from 'react';
import { ProjectBodyProps } from './props';

export const ProjectBody = (props: ProjectBodyProps) => {
  const { project } = props;
  const uId = useId();

  return <></>;
};

export default ProjectBody;
